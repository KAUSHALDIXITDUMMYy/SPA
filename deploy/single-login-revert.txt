# Revert target: single-login / multi-device lock (last-login-wins)
#
# STATUS (2026-07-23): REVERTED on request — website logout was breaking mobile auth.
#   Web revert commit:    bccefd4  (reverts c1d4af4)
#   Mobile revert commit: d00d95f  (reverts 920a74b)
#   VPS + Vercel redeployed after revert.
#
# If we re-enable one-login later, use a safer design that does NOT clear
# server sessionId on web logout in a way that invalidates the other device,
# and does not require X-Session-Id on every API until both clients are updated.
#
# --- Original revert recipe (kept for reference) ---
# WEB session lock commit: c1d4af4 → git revert c1d4af4
# MOBILE session lock commit: 920a74b → git revert 920a74b
